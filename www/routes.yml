/:
  controller: Base
  action: index


/login:
  controller: Auth
  action: login


/register:
  controller: Auth
  action: register


/password_reset:
  controller: Auth
  action: password_reset


/verify:
  controller: Auth
  action: verify


/reset_password:
  controller: Auth
  action: reset_password


/home:
  controller: Base
  action: home


/admin:
  controller: Admin
  action: index


/manage_users:
  controller: Admin
  action: manage_users