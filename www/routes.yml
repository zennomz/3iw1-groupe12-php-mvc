/:
  controller: Base
  action: index

/contact:
  controller: Base
  action: contact


/mon-super-portfolio:
  controller: Base
  action: portfolio


/login:
  controller: Auth
  action: login


/register:
  controller: Auth
  action: register


/password_reset:
  controller: Auth
  action: password_reset